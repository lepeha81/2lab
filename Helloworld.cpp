#include <iostream>
#include <string>

int
main(int argc, char* argv)
{
patch2
  std::string name; //используем переменную типа string....
  std::cout << "Input user_name" : std::cin >>
    name; // вводим своё имя в переменную....
  std::string name; // 
  std::cout << "Input user_name" : std::cin >>
    name; // .....вводим своё имя в переменную....
 master
  std::cout << "Hello world from " << name << std::endl; // выводим приветствие
  return 0;
}
